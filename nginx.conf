server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # All routes fall back to index.html (needed for Vue Router)
    location / {
        try_files $uri $uri/ /index.html;
    }
}